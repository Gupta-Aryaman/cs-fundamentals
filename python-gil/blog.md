[//]: <MarkDown Preview Showdown (MDPS), vscode extension>
### Hi

Python is multithreaded, but the 2 threads cannot execute simultaneouly becuase of the GIL - Global Interpreter Lock.

```
print("hello world")
```
